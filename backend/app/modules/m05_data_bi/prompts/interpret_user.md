# 用户问题

{question}

# 时间范围

{time_range_desc}

# 已执行的只读 SQL

{sql}

# 查询结果（真实数据，共 {row_count} 行；超过 {preview_limit} 行仅展示前 {preview_limit} 行用于解读，图表使用完整数据）

{rows_json}

# 任务

基于上述真实数据输出结论解读、归因分解与图表配置。严格遵守系统提示词的 JSON 格式，数值必须与查询结果一致，不得编造。
